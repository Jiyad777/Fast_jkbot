{
    "name": "MadeBy MKN",
    "description": "When you going to send file on telegram channel this bot will save that in database, So you can search that easily in inline mode",
    "stack": "container",
    "keywords": ["Bot", "telegram"],
    "website": "https://github.com",
    "repository": "https://github.com",
    "env": {
        "CHANNELS": {
            "description": "Username or ID of channel or group. Separate multiple IDs by space.",
            "required": false
        },
        "PICS": {
            "description": "Add some telegraph link of pictures .",
            "required": false
        },
        "LOG_CHANNEL": {
            "description": "Bot Logs,Give a channel id with -100xxxxxxx",
            "required": true
        },
        "AUTH_USERS": {
            "description": "Username or ID of users to give access of inline search. Separate multiple users by space.\nLeave it empty if you don't want to restrict bot usage.",
            "required": false
        },
        "AUTH_CHANNEL": {
            "description": "ID of channel.Make sure bot is admin in this channel. Without subscribing this channel users cannot use bot.",
            "required": false
        },
        "REQ_LINK": {
            "description": "optional: add request link if your channel is public? to skil this",
            "required": false
        },
        "AUTH_GROUPS": {
            "description": "Auth group add if group id bot will working only that group",
            "required": false 
        },
        "SUPPORT_CHAT": {
            "description": "group username without @",
            "required": false
        },
        "CUSTOM_FILE_CAPTION": {
            "description": "Custom FIle Caption. key: {file_name}, {file_size}",
            "value": "{file_name}",
            "required": false
        }
    },
    "addons": [],
    "buildpacks": [{ "url": "heroku/python"}],

}
